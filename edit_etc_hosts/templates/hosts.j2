{# Templatka dla /etc/hosts file #}

{% for host in groups['all'] %}
{{ hostvars[host]['ansible_facts']['default_ipv4']['address'] }} {{ hostvars[host]['ansible_facts']['hostname'] }}
{% endfor %}


#{{ ansible_facts['fqdn'] }} or {{ inventory_hostname }}
